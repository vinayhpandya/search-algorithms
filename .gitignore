# OpenSearch Docker volume (regenerated by `docker compose up`, never commit)
opensearch-data/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Env / secrets
.env

# HuggingFace / model weight caches (downloaded fresh on first run)
.cache/
**/hf_cache/

# Downloaded PDFs, if we ever add full-text as a stretch goal
data/pdfs/
data/raw/**

# Jupyter checkpoints
.ipynb_checkpoints/
eval/candidates.json
eval/cluster_centroids.json
eval/semantic_ids.json
eval/semantic_id_model.tar.gz
eval/synthetic_queries.json
eval/semantic_id_model_extracted/
README_files/

README.html
# OS cruft
.DS_Store